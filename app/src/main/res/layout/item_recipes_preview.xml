<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/iv_recipe_preview"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:background="@drawable/category_image"
        />

    <TextView
        android:id="@+id/tv_recipe_preview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/iv_recipe_preview"
        app:layout_constraintStart_toStartOf="@id/iv_recipe_preview"
        app:layout_constraintEnd_toEndOf="@id/iv_recipe_preview"
        android:textSize="12sp"
        tools:text="이름이 20자가 넘어갈 때 어떻게 되는지 한번 보자"
        android:fontFamily="@font/noto_sans"
        android:textColor="@color/jipdabaing_black"
        android:layout_marginTop="6dp"
        android:maxWidth="96dp"
        android:minLines="2"
        android:maxLines="2"
        android:gravity="center"
        android:ellipsize="end"
        />

    <TextView
        android:id="@+id/tv_likes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/tv_recipe_preview"
        app:layout_constraintStart_toStartOf="@id/iv_recipe_preview"
        app:layout_constraintEnd_toEndOf="@id/iv_recipe_preview"
        app:layout_constraintBottom_toBottomOf="parent"
        android:drawableLeft="@drawable/like_filled"
        android:maxLines="1"
        android:layout_marginBottom="24dp"
        tools:text="123456"
        android:gravity="center"
        />

</androidx.constraintlayout.widget.ConstraintLayout>