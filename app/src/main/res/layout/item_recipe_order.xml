<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:id="@+id/layout_order_step"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_order_step"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Step "
            android:textColor="@color/jipdabaing_black"
            android:textSize="18sp"
            android:fontFamily="@font/noto_sans"/>

        <TextView
            android:id="@+id/tv_order_num"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="num"
            android:textColor="@color/jipdabaing_black"
            android:textSize="18sp"
            android:fontFamily="@font/noto_sans"/>

    </LinearLayout>

    <TextView
        android:id="@+id/tv_order_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/layout_order_step"
        app:layout_constraintTop_toBottomOf="@id/layout_order_step"
        android:layout_marginTop="6dp"
        tools:text="만드는 방법 1단계를 설명해주세요."
        android:textColor="@color/jipdabaing_black"
        android:textSize="18sp"
        android:fontFamily="@font/noto_sans" />

    <ImageView
        android:id="@+id/iv_order_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        app:layout_constraintTop_toBottomOf="@id/tv_order_description"
        android:layout_marginTop="6dp"
        tools:src="@drawable/img"/>

</androidx.constraintlayout.widget.ConstraintLayout>