/*package com.example.umc_zipdabang.src.my

import android.os.Bundle
import com.example.umc_zipdabang.databinding.ActivityMyWritingBinding

class MyWriting2Activity {

    private lateinit var viewBinding: ActivityMyWritingBinding
    viewBinding = ActivityMyWritingBinding.inflate(layoutInflater)

    if(list[0]!="" && viewBinding.myRecipeEditTital.text.toString>0 &&
    viewBinding.myRecipeEditTime.text >0 &&  viewBinding.myRecipeEditDescribe.text >0
    viewBinding.myRecipeEditIngredient_tital.text >0 && viewBinding.myRecipeEditIngredient_quan.text >0
    viewBinding.myRecipeEditAftertip.text >0 && viewBinding.myRecipeEditStepDescribe.text >0
    && 카테고리선택 && list[1]!=""){
        viewBinding.mybtn.isClickable = true

        if(num == 2){
            viewBInding.myRecipeIngredient_name.text >0
            viewBInding.myRecipeIngredient_quen.text >0
            viewBinding.mybtn.isClickable = true
            if(num ==3){

            }
        }

        if(num2 == 2){
            list[2]!=""
            viewBinding.myRecipeStep2Tv.text >0
            viewBinding.mybtn.isClickable = true
        }
    }

    //view가 gone이 아니라 visible 상태인 애들에 대한 것만 써지고 + 기본적인거 채우면 업로드 버튼 활성화


}*/